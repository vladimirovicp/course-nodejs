# Оценивание: основы Node.js

Максимальный общий балл: 206


## Проверка
Для упрощения проверки у вас есть `npm`-скрипты в `package.json`.  
NB! Некоторые скрипты содержат предопределённые данные (например, переменные окружения, аргументы CLI). При необходимости смело изменяйте их во время проверки.

## Базовая область

- Файловая система (`src/fs`)
    - **+6** `create.js` реализован корректно
    - **+10** `copy.js` реализован корректно
    - **+10** `rename.js` реализован корректно
    - **+6** `delete.js` реализован корректно
    - **+6** `list.js` реализован корректно
    - **+6** `read.js` реализован корректно
- Интерфейс командной строки (`src/cli`)
    - **+6** `env.js` реализован корректно
    - **+6** `args.js` реализован корректно
- Модули (`src/modules`)
    - **+20** `cjsToEsm.cjs` корректно отрефакторен
- Хеширование (`src/hash`)
    - **+10** `calcHash.js` реализован корректно
- Потоки (`src/streams`)
    - **+10** `read.js` реализован корректно
    - **+10** `write.js` реализован корректно
    - **+10** `transform.js` реализован корректно
- Zlib (`src/zip`)
    - **+10** `compress.js` реализован корректно
    - **+10** `decompress.js` реализован корректно

## Продвинутая область

- Worker Threads (`src/wt`)
    - **+10** `worker.js` реализован корректно
    - **+30** `main.js` реализован корректно
- Дочерние процессы (`src/cp`)
    - **+10** создаёт дочерний процесс
    - **+10** `stdin` дочернего процесса получает ввод из `stdin` мастер-процесса
    - **+10** `stdout` дочернего процесса отправляет данные в `stdout` мастер-процесса

## Штрафы

- **-95% от общего балла за задание** Используются любые внешние инструменты/библиотеки
- **-30% от общего балла за задание** Коммиты после дедлайна (кроме коммитов, затрагивающих только `Readme.md`, `.gitignore` и т. п.)
